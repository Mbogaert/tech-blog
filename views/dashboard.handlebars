
{{#if posts.length}}
<section>
  <h2>Your Dashboard</h2>
  <ol>
    {{#each posts as |post|}}
    <li>
      {{> post-title post}}
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

<button type="submit" class="btn"><a class="link" href="/dashboard/new">New Post</a></button>