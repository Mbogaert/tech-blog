<article class="post">
  <div class="title">
    <a href="/post/{{id}}">{{title}}</a>
    <span>Created by {{user.username}} on {{created_at}}</span>
  </div>
  <div class="meta">
      ({{content}})
  </div>
</article>